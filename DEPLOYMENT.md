# Инструкция по деплою StatApp в Vercel

## Шаг 1: Подготовка кода

1. Скачайте проект через GitHub или Download ZIP кнопку в v0
2. Распакуйте архив
3. Откройте терминал в папке проекта

## Шаг 2: Создайте GitHub репозиторий

### Через GitHub веб-интерфейс:
1. Зайдите на [github.com](https://github.com)
2. Нажмите на "+" в правом верхнем углу → "New repository"
3. Введите название репозитория (например, `statapp`)
4. Оставьте репозиторий публичным
5. НЕ добавляйте README, .gitignore или лицензию (они уже есть в проекте)
6. Нажмите "Create repository"

### Загрузите код в GitHub:
\`\`\`bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: StatApp for Farcaster"

# Переключитесь на main ветку
git branch -M main

# Добавьте удаленный репозиторий (замените YOUR_USERNAME и YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Загрузите код
git push -u origin main
\`\`\`

## Шаг 3: Деплой в Vercel

### Способ А: Через веб-интерфейс (проще)

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub аккаунт
3. Нажмите "Add New..." → "Project"
4. Найдите ваш репозиторий `statapp` в списке
5. Нажмите "Import"
6. Vercel автоматически определит Next.js проект
7. **НЕ нажимайте Deploy пока!** Сначала добавьте переменные окружения (см. Шаг 4)

### Способ Б: Через Vercel CLI

\`\`\`bash
# Установите Vercel CLI глобально
npm install -g vercel

# Запустите деплой
vercel

# Следуйте инструкциям:
# - Set up and deploy? Yes
# - Which scope? Выберите ваш аккаунт
# - Link to existing project? No
# - What's your project's name? statapp
# - In which directory is your code located? ./
# - Want to override settings? No
\`\`\`

## Шаг 4: Настройка переменных окружения

### Получите подпись для Farcaster Manifest:

1. Откройте [base.dev](https://base.dev) в браузере
2. Нажмите "Sign In" и войдите через Base Account
3. После входа перейдите в раздел **"Preview"** → **"Account Association"**
4. В поле "App URL" введите ваш Vercel домен (например, `statapp-abc123.vercel.app`)
   - Если вы еще не задеплоили, используйте временный URL из Vercel preview
5. Нажмите **"Submit"**
6. Нажмите **"Verify"**
7. Нажмите **"Sign"** и подтвердите транзакцию в вашем кошельке
8. После подписи вы увидите три значения:
   - `header`
   - `payload`
   - `signature`
9. **Скопируйте эти значения** - они понадобятся на следующем шаге

### Добавьте переменные окружения в Vercel:

#### В веб-интерфейсе Vercel:

1. Откройте ваш проект в Vercel Dashboard
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте следующие переменные:

   **Переменная 1:**
   - Name: `FARCASTER_HEADER`
   - Value: (вставьте значение header из base.dev)
   - Environment: Production, Preview, Development (выберите все)
   - Нажмите "Save"

   **Переменная 2:**
   - Name: `FARCASTER_PAYLOAD`
   - Value: (вставьте значение payload из base.dev)
   - Environment: Production, Preview, Development
   - Нажмите "Save"

   **Переменная 3:**
   - Name: `FARCASTER_SIGNATURE`
   - Value: (вставьте значение signature из base.dev)
   - Environment: Production, Preview, Development
   - Нажмите "Save"

   **Переменная 4 (опционально):**
   - Name: `NEXT_PUBLIC_APP_URL`
   - Value: `https://ваш-домен.vercel.app` (ваш полный URL)
   - Environment: Production, Preview, Development
   - Нажмите "Save"

4. После добавления всех переменных, вернитесь на вкладку **"Deployments"**
5. Найдите последний деплой и нажмите на три точки справа
6. Выберите **"Redeploy"** → **"Redeploy"** (чтобы применить переменные окружения)

## Шаг 5: Проверка деплоя

1. Дождитесь завершения деплоя (обычно 1-2 минуты)
2. Откройте ваш сайт по ссылке (например, `https://statapp-abc123.vercel.app`)
3. Проверьте, что приложение загружается без ошибок
4. Откройте в браузере: `https://statapp-abc123.vercel.app/.well-known/farcaster.json`
   - Вы должны увидеть JSON с заполненными `header`, `payload`, `signature`
   - Если они пустые, значит переменные окружения не применились - выполните редеплой

## Шаг 6: Проверка в Farcaster

1. Откройте мобильное приложение Farcaster
2. Вставьте ссылку на ваше приложение в композер поста
3. Должен появиться preview с иконкой и кнопкой "View Stats"
4. Нажмите на кнопку - должно открыться Mini App

## Решение проблем

### Проблема: Splash screen зависает
**Решение:** Проверьте, что `sdk.actions.ready()` вызывается в коде. Файл `hooks/use-farcaster-context.ts` уже содержит этот вызов.

### Проблема: "No valid account association"
**Решение:** 
1. Убедитесь, что все три переменные окружения добавлены в Vercel
2. Проверьте, что значения скопированы полностью без лишних пробелов
3. Выполните редеплой проекта в Vercel

### Проблема: "Ready not called"
**Решение:** Откройте DevTools консоль и проверьте ошибки. Убедитесь, что Farcaster SDK загружается корректно.

### Проблема: Статистика не загружается
**Решение:** В текущей версии используются моковые данные. Для получения реальных данных нужно интегрировать Farcaster Hub API в файле `app/api/stats/[fid]/route.ts`.

## Дополнительная настройка

### Кастомный домен

1. В Vercel Dashboard откройте ваш проект
2. Перейдите в **Settings** → **Domains**
3. Нажмите **"Add"**
4. Введите ваш домен и следуйте инструкциям по настройке DNS

### После добавления кастомного домена:

1. Обновите переменную окружения `NEXT_PUBLIC_APP_URL` на новый домен
2. Вернитесь на [base.dev](https://base.dev) и повторите процесс подписи манифеста для нового домена
3. Обновите переменные `FARCASTER_HEADER`, `FARCASTER_PAYLOAD`, `FARCASTER_SIGNATURE`
4. Выполните редеплой

## Готово!

Теперь ваше приложение StatApp задеплоено и готово к использованию в Farcaster! 🎉

Если возникли проблемы, проверьте логи в Vercel Dashboard → Functions → View Function Logs.
